{
  "name": "sentra",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "mysql2": "^3.14.2"
  },
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "dev:web": "npm run dev -w @sentra/web",
    "dev:api": "npm run dev -w @sentra/api",
    "build": "npm run build --workspaces --if-present",
    "db:create": "node scripts/create-system-db.mjs",
    "db:migrate": "npm run db:migrate -w @sentra/api",
    "db:seed": "npm run db:seed -w @sentra/api",
    "db:setup": "npm run db:create && npm run db:migrate && npm run db:seed",
    "assets:sync-electorals": "node scripts/sync-electorals-assets.mjs"
  }
}
